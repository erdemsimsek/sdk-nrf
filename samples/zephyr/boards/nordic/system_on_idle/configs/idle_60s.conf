# Raise the idle duration to 60 seconds (from the default 5), for either
# the SERIAL timed loop or CONFIG_NRF7120_QUIET_PERIODIC_SLEEP. Tests
# whether a longer, still-bounded idle window is enough to reach ELV.
CONFIG_NRF7120_IDLE_SECONDS=60
