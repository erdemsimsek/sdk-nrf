# Loop a bounded k_sleep forever, with no diagnostics/console/GPIO-marker
# activity around each wake at all. Isolates whether periodic waking
# itself -- not the diagnostics build's console/print overhead -- is what
# prevents reaching ELV. Combine with configs/quiet.conf.
CONFIG_NRF7120_QUIET_PERIODIC_SLEEP=y
