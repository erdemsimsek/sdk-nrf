# Repoint LP hyst voltage target from VOUT0V8LP to VOUT0V65.
# MODECTRL stays Auto; the existing HP<->LP<->HP bounce still happens on
# its own, now with a scope-visible voltage swing.
CONFIG_NRF7120_HVBUCK_VOLTAGECTRL_MANUAL=y
