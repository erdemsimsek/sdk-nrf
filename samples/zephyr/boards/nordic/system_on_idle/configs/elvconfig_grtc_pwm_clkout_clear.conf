# Isolated test (Jon Harrison): clear only ELVCONFIG.ELVGRTCPWM (bit 22) and
# ELVCONFIG.ELVGRTCCLKOUT (bit 23), leaving every other bit -- including the
# currently-set ELVPDLP/ELVPDPERIPH -- untouched.
CONFIG_NRF7120_ELVCONFIG_GRTC_PWM_CLKOUT_CLEAR=y
